services:
  - type: web
    name: apuntes-ia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --timeout 300 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8

